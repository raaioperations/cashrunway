{
  "name": "cashrunway",
  "version": "0.1.0-foundation",
  "private": true,
  "description": "CashRunway V1 foundation workspace",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "validate": "sh scripts/validate.sh",
    "test:offline": "playwright test tests/offline.runtime.spec.js --config=playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
